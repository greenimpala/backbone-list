define([
	"backbone",
	"underscore",
	"jquery",
	"components/tree/model/TreeItem"
], function (Backbone, _, $, TreeItem) {
	var File = TreeItem.extend({
		initialize: function () {

		}
	});

	return File;
});